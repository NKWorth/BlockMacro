namespace MacroBlocks.Models.Actions;

/// <summary>
/// A block that performs an input action when reached during playback
/// (delay, mouse, keyboard, etc.).
/// </summary>
public abstract class ActionBlock : MacroBlock
{
}
